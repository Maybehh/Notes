cookie存储了用户在某一个网页上的输入的个人信息等，在客户端中，访问页面时网址和cookie一同请求服务器，服务器就可以返回有用户信息的网页，
通过cookie,服务器容易找到有用户信息的对应网页，保留的信息(记住账号之类的)
session(里面的内容类似cookie里的内容)，但是session在服务器里，session给了一个sessionid在cookie上
(因为cookie不安全信息容易被偷，锁id比较安全)，通过cookie找到sessionid然后找到信息
本地storage是把cookie的内容存储在客户端，然后网页在呈现后，通过js获取其中的数据
(区别:cookies是需要送至服务器，服务器解析后返回有信息的网页，只能存储些许数据，
本地存储是网页返回后，通过js获取客户端的信息进行渲染，无需经过服务器，可存储大量数据，且更安全，不过还是有可能会被攻破)
